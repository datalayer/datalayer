import { Meta, Canvas } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';

import { BoxPanelReact } from '@jupyter-react/lumino-adapter/lib'
import { CellExample } from '@jupyter-react/jupyterlab-examples/lib';

import Button from '@material-ui/core/Button';
import CssBaseline from '@material-ui/core/CssBaseline';
import { ThemeProvider } from '@material-ui/core/styles';

import { muiLightTheme } from '@jupyter-react/light-theme';

<Meta title="JupyterLab/Cell" />

# JupyterLab Cell

<Canvas>
  <Story name="Simple Cell">
    <ThemeProvider theme={muiLightTheme}>
      <CssBaseline />
      <Button variant="contained" color="primary" onClick={action('material ui clicked')}>
        Top Widget
      </Button>
      <div>Hello JupyterLab Cell</div>
      <BoxPanelReact style={{ width: 500 }}>
        <CellExample />
      </BoxPanelReact>
      <div>Bye JupyterLab Cell</div>
      <Button variant="contained" color="primary" onClick={action('material ui clicked')}>
        Bottom Widget
      </Button>
    </ThemeProvider>
  </Story>
</Canvas>
