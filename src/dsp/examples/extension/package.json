{
  "name": "@datalayer/example-extension",
  "private": true,
  "scripts": {
    "start": "lerna run --scope @datalayer/* --parallel start",
    "build": "lerna run --scope @datalayer/* build",
    "serve": "lerna run --scope @datalayer/* --parallel serve",
    "clean": "lerna run --scope @datalayer/* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.20.2"
  },
  "workspaces": {
    "packages": [
      "app1",
      "app2",
      "graphql"
    ]
  }
}
