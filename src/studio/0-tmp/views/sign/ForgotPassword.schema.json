{
  "name": "forgotpassword",
  "plural": "forgotpasswords",
  "label": "Forgot Password",
  "fields": {
    "username": {
      "name": "username",
      "label": "Username",
      "type": "string",
      "length": [
        3,
        20
      ],
      "pattern": "^[A-Za-z0-9_]+$",
      "errorMessage": "Only allow alphanumeric characters (A-Z, a-z, 0-9) and underscore (_)",
      "required": true
    },
    "password": {
      "name": "password",
      "label": "Password",
      "required": true,
      "pattern": "^[A-Za-z0-9_]+$",
      "errorMessage": "Only allow alphanumeric characters (A-Z, a-z, 0-9) and underscore (_)",
      "type": "password",
      "length": [
        6,
        100
      ]
    },
    "password_confirm": {
      "name": "password_confirm",
      "label": "Password Confirmation",
      "required": true,
      "pattern": "^[A-Za-z0-9_]+$",
      "errorMessage": "Only allow alphanumeric characters (A-Z, a-z, 0-9) and underscore (_)",
      "type": "password",
      "length": [
        6,
        100
      ]
    }
  }
}
