apiVersion: v1
kind: Pod
metadata:
  name: constraintpod
spec:
  containers:
  - name: dla
    image: datalayer/simple:0.0.3
    ports:
    - containerPort: 9876
    resources:
      limits:
        cpu: "30m"
        memory: "250Mi" 
