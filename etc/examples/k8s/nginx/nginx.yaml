apiVersion: v1
kind: Deployment
metadata:
  name: nginx-deployment
  namespace: default
spec:
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nginx:1.7.9
        ports:
        - containerPort: 80
